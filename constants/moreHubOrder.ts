/** 더보기 탭에서 이동하는 스택 화면 키(순서 저장용). 캘린더는 헤더에서만 진입. */
export type MoreHubRouteKey = 'account' | 'briefing' | 'settings';

export const MORE_HUB_ROUTE_ORDER_DEFAULT: MoreHubRouteKey[] = ['account', 'briefing', 'settings'];
