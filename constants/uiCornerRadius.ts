/**
 * Less-rounded UI corners — global shape scale.
 * Rollback: revert the commit that introduced this file and its imports.
 */

/** 플로팅 탭바 캡슐 — was 30 */
export const UI_RADIUS_TAB_BAR_FLOAT = 22;

/** 그룹 피드 카드(뉴스·공시 리스트) — was 22 */
export const UI_RADIUS_GROUPED_FEED = 16;

/** 인-화면 세그먼트 pill 외곽 — was 10 */
export const UI_RADIUS_SEGMENT_OUTER = 8;

/** 인-화면 세그먼트 pill 버튼 — was 8 */
export const UI_RADIUS_SEGMENT_BTN = 6;

/** 일반 카드·타일 — was 12~14 */
export const UI_RADIUS_CARD = 10;

/** 큰 카드(히어로·다이제스트 단일) — was 16~18 */
export const UI_RADIUS_CARD_LG = 12;

/** 시트·모달 상단 — was 18~22 */
export const UI_RADIUS_SHEET = 14;

/** 다이제스트 카드(wide) — was 16 */
export const UI_RADIUS_DIGEST_SINGLE = 12;

/** 다이제스트 카드(pair) — was 12 */
export const UI_RADIUS_DIGEST_PAIR = 10;
